services:
  - type: web
    name: nairobi-dashboard
    runtime: static
    rootDir: nairobi-dashboard
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html